training_data_paths:
- "/data/cuizhengliang/dataset/chronos_datasets/training_corpus/kernel_synth_1m.arrow"
- "/data/cuizhengliang/dataset/chronos_datasets/training_corpus/tsmixup_10m.arrow"
probability:
- 0.9
- 0.1
context_length: 512
prediction_length: 64
min_past: 60
max_steps: 20_000
save_steps: 10_000
log_steps: 500
per_device_train_batch_size: 32
learning_rate: 0.001
optim: adamw_torch_fused
num_samples: 20
shuffle_buffer_length: 10_000
gradient_accumulation_steps: 1
model_id: /data/cuizhengliang/chronos-forecasting/scripts/ckpt/seagull
model_type: causal
random_init: false
tie_embeddings: true
output_dir: ./output/
fp16: false
torch_compile: true
tokenizer_class: "MeanScaleUniformBins"
tokenizer_kwargs:
  low_limit: -15.0
  high_limit: 15.0
n_tokens: 4096
lr_scheduler_type: linear
warmup_ratio: 0.0
dataloader_num_workers: 4
max_missing_prop: 0.1
use_eos_token: true

